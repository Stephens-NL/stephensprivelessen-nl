# Google Drive API Configuration for Aantekeningen
# Get these values from Google Cloud Console

# Service Account Email (required)
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service Account Private Key (required)
# Copy the entire private key from the JSON file, including the BEGIN/END lines
GOOGLE_SERVICE_ACCOUNT_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Notability Folder Configuration (choose one)
# Option 1: Use folder ID (recommended - faster and more reliable)
NOTABILITY_FOLDER_ID=your-notability-folder-id-here

# Option 2: Use folder name (fallback)
# NOTABILITY_FOLDER_NAME=Notability

# Optional: Rate limiting configuration
RATE_LIMIT_MAX_REQUESTS=30
RATE_LIMIT_WINDOW_MS=60000

# Optional: Cache configuration
CACHE_DURATION_MS=60000
